% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spread.R
\name{yc_key_rate_duration}
\alias{yc_key_rate_duration}
\title{Key Rate Durations}
\usage{
yc_key_rate_duration(
  coupon_rate,
  maturity,
  curve,
  key_rates = c(1, 2, 5, 10, 30),
  shift = 1e-04,
  face = 100,
  frequency = 2
)
}
\arguments{
\item{coupon_rate}{Numeric. Annual coupon rate as a decimal.}

\item{maturity}{Numeric. Time to maturity in years.}

\item{curve}{Either a \code{yc_curve} object or a list/data frame with
components \code{maturities} and \code{rates}.}

\item{key_rates}{Numeric vector of key rate tenors in years.
Default is \code{c(1, 2, 5, 10, 30)}.}

\item{shift}{Numeric. Size of the rate bump in decimal (default 0.0001,
i.e. 1 basis point).}

\item{face}{Numeric. Face value. Default is 100.}

\item{frequency}{Integer. Coupon frequency: 1 (annual) or 2
(semi-annual, default).}
}
\value{
A data frame with columns \code{tenor} and \code{key_rate_duration}.
}
\description{
Compute key rate durations by bumping the yield curve at specific tenors.
Each bump is triangular: the full shift is applied at the key rate tenor
and linearly interpolated to zero at adjacent key rate tenors.
}
\examples{
curve <- yc_curve(c(1, 2, 5, 10, 30), c(0.03, 0.035, 0.04, 0.042, 0.045))
yc_key_rate_duration(coupon_rate = 0.04, maturity = 10,
                     curve = curve, key_rates = c(1, 2, 5, 10, 30))
}
