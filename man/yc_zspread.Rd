% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spread.R
\name{yc_zspread}
\alias{yc_zspread}
\title{Z-Spread}
\usage{
yc_zspread(price, coupon_rate, maturity, curve, face = 100, frequency = 2)
}
\arguments{
\item{price}{Numeric. Market price of the bond.}

\item{coupon_rate}{Numeric. Annual coupon rate as a decimal.}

\item{maturity}{Numeric. Time to maturity in years.}

\item{curve}{Either a \code{yc_curve} object or a list/data frame with
components \code{maturities} and \code{rates}.}

\item{face}{Numeric. Face value of the bond. Default is 100.}

\item{frequency}{Integer. Coupon frequency per year: 1 for annual or
2 for semi-annual (default).}
}
\value{
A list with components \code{zspread} (the Z-spread as a decimal),
\code{price} (the input price), and \code{model_price} (the price implied by the
curve with the Z-spread applied).
}
\description{
Compute the Z-spread (zero-volatility spread) for a bond. The Z-spread
is the constant spread added to each zero rate on the benchmark curve
that makes the discounted cash flows equal the market price.
}
\examples{
# Create a benchmark curve
curve <- yc_curve(c(0.5, 1, 2, 5, 10), c(0.03, 0.035, 0.04, 0.042, 0.045))

# A bond priced below par (positive Z-spread)
yc_zspread(price = 95, coupon_rate = 0.04, maturity = 5,
           curve = curve, frequency = 2)
}
